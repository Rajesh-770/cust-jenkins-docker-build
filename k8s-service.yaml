apiVersion: v1
kind: Service
metadata:
  name: cust-app-service
  labels:
    app: cust-app
spec:
  type: NodePort
  selector:
    app: cust-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
